<h1>Users#index</h1>

<table>
	<thead>
		<tr>
			<th>Email</th>
			<th>Name</th>
		</tr>
	</thead>
	<tbody>
		<% @user.each |user| %>
			<tr>
				<td><%= user.email %></td>
				<td><%= user.name %></td>
			</tr>
		<% end %>
	</tbody>
</table>
<% @user.each |user| %>
	

<% end %>
<%= link_to "New User", new_user_path %>